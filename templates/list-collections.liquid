<ul>
    {% for collection in collections %}
    {% unless collection.handle == 'frontpage' %}
	<li><a href="{{ collection.url | sort_by:'created-ascending' }}">{{ collection.title }}</a>
    {% endunless %}
    {% endfor %}
</ul>